import React, { Component } from "react";
import styled from "styled-components";

class TopBar extends Component {
  render() {
    return (
      <div container>
        <div color="default">
          <div item>Get Out There</div>
        </div>
      </div>
    );
  }
}

export default TopBar;
